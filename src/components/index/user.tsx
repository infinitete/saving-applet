import { View, Text } from "@tarojs/components";
import React from "react";

const User: React.FC = () => {
  return (
    <View className="user-page">
      <Text>User</Text>
    </View>
  );
};

export default User;
